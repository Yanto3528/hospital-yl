import { printDoctor } from '@hospital-yl/doctor';
import { printPatient } from '@hospital-yl/patient';
import { printScheduler } from '@hospital-yl/scheduler';

console.log(printDoctor());
console.log(printPatient());
console.log(printScheduler());
